import { ComingSoon } from "../_components/ComingSoon";

export default function ToolsPage() {
  return (
    <ComingSoon
      title="Tools & Equipment Spend"
      body="Purchase log + cost vs budget. Replaces the Google Form / Sheet."
      schemaNote="warehouse_tool_purchases + warehouse_budgets"
    />
  );
}

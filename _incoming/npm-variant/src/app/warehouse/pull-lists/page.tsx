import { ComingSoon } from "../_components/ComingSoon";

export default function PullListsPage() {
  return (
    <ComingSoon
      title="Pull Lists"
      body="Daily material assembly per job. Schema is in place; UI is the next build target."
      schemaNote="warehouse_pull_lists + warehouse_pull_list_rolls"
    />
  );
}
